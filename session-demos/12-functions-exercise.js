// Write a function square() that returns the square of a number passed to it. Use function declaration syntax to declare the function.
// Example:

// console.log( square( 3 ) ); // prints 9

function square( n ) {
    return n * n;
}

console.log( square( 3 ) );